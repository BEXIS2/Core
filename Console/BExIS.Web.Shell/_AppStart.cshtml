@using Vaiona.Utils.Cfg;
@using Microsoft.Web.Helpers;
@{
    //string s = VirtualPathUtility.ToAppRelative(AppConfiguration.ThemesPath);
    Themes.Initialize(AppConfiguration.ThemesPath, AppConfiguration.DefaultThemeName);
}
