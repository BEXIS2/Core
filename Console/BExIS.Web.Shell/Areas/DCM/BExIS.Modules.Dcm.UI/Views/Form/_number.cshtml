@model BExIS.Modules.Dcm.UI.Models.Metadata.UIComponentModel
@using BExIS.Modules.Dcm.UI.Models.Metadata

@{
    string action = "OnChange(this)";
    if (Model.Type.Equals(MetadataAttrType.Parameter)) { action = "OnChangeParameter(this)"; }

}

@if (!Model.Locked)
{
@*@Html.TextBoxFor(m => m.Value,
    new
    {
        idbyxpath = @Model.IdByXpath,
        OnChange = @action,
        Name = @Model.DisplayName,
        Id = @Model.IdInput,
        PackageId = @Model.ParentId,
        Title = @Model.ErrorMessage,
        @class = "bx-input bx-metadataFormTextInput " + @Model.ErrorClass
    })*@

<input class="bx-input" id="@Model.IdInput" idbyxpath="@Model.IdByXpath" name="@Model.IdInput" title="" value="@Model.Value" onchange="@action" />
}
else
{
@Html.Partial("_displayonly", Model)
}