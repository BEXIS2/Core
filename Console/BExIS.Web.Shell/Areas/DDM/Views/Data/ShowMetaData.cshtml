@{
    ViewBag.Title = "Metadata";
}

<br />
@if (ViewData.ModelState.IsValid)
{ 
    @(new HtmlString(Session["Metadata"].ToString()))
}
else
{ 
    @Html.ValidationSummary(true)
}
<br />