@using BExIS.Modules.Sam.UI.Models
@using Telerik.Web.Mvc.UI

@(Html.Telerik().PanelBar()
        .Name("panelBarEntities")
    .Items(items =>
    {
        items.Add().Text("ABC");
        items.Add().Text("DEF");
        items.Add().Text("HIJ");
    })
)