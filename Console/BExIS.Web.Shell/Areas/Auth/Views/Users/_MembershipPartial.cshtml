@using Telerik.Web.Mvc.UI;
@using BExIS.Web.Shell.Areas.Auth.Models;

@model List<UserRoleModel>

<script src="@Url.Content("~/Scripts/jquery-1.7.1.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.unobtrusive-ajax.js")" type="text/javascript"></script>

@(Html.Telerik().Grid(Model)
    .Name("Grid_UserMembership")
    .HtmlAttributes(new { style = "width: 800px" } )
    .DataKeys(keys =>
    {
        keys.Add(r => r.Id);
    })
    .Columns(columns =>
    {
        columns.Bound(r => r.Id)
            .ClientTemplate("<input type='checkbox' onclick='setMembership(this.checked, <#= UserId #>, <#= Id #>);' name='nothing' <#= UserInRole ? checked='checked' : '' #> />")
                   .Title("Check")
                   .Width(50)
                   .HtmlAttributes(new { style = "text-align:center" });
        columns.Bound(r => r.Id).Width(100);
        columns.Bound(r => r.RoleName).Width(200);
        columns.Bound(r => r.Description);
    })
    .DataBinding(dataBinding => dataBinding
        .Ajax()
        .Select("UserMembership_Select", "Users", (long)ViewData["UserID"])
    )
    .Filterable()
    .Sortable(sortable => sortable
        .OrderBy(orderby => orderby
            .Add(r => r.RoleName)
            .Ascending()
        )
    )
    .Scrollable(scrollable => scrollable.Height(475))
)