<?xml version="1.0" encoding="utf-8" ?>
<Map>
	<MapItem ContentKey="Menu">
		<ContentProviders>
			<ContentProvider View="_Menu" Type="View" Enabled="true">
				<Parameters>
				</Parameters>
			</ContentProvider>
			<!--to be deleted-->
			<ContentProvider Area="" Controller="Nav" Action="Menu" Type="Action" Enabled="false">
				<Parameters>
					<Parameter Name="oreintation" Value="1" />
				</Parameters>
			</ContentProvider>
		</ContentProviders>
	</MapItem>

	<MapItem ContentKey="Header">
		<ContentProviders>
			<ContentProvider  Area="" Controller="Header" Action="Index" Type="Action" Enabled="true">
				<Parameters>
				</Parameters>
			</ContentProvider>
		</ContentProviders>
	</MapItem>

	<MapItem ContentKey="Breadcrumb">
		<ContentProviders>
			<ContentProvider View="_Breadcrumb" Type="View" Enabled="true">
				<Parameters>
				</Parameters>
			</ContentProvider>
		</ContentProviders>
	</MapItem>

	<!--<MapItem ContentKey="Information">
		<ContentProviders>
			<ContentProvider View="_Information" Type="View" Enabled="true">
				<Parameters>
				</Parameters>
			</ContentProvider>
		</ContentProviders>
	</MapItem>-->

	<MapItem ContentKey="LogOnStatus">
		<ContentProviders>
			<ContentProvider View="_LogOnStatus" Type="View" Enabled="true">
				<Parameters>
				</Parameters>
			</ContentProvider>
		</ContentProviders>
	</MapItem>

	<MapItem ContentKey="ThemeSelector">
		<ContentProviders>
			<ContentProvider View="_ThemeSelector" Type="View" Enabled="false">
				<!--changes are applied after application restart -->
				<Parameters>
				</Parameters>
			</ContentProvider>
		</ContentProviders>
	</MapItem>

	<MapItem ContentKey="Footer">
		<ContentProviders>
			<ContentProvider  Area="" Controller="Footer" Action="Index" Type="Action" Enabled="true">
				<Parameters>
				</Parameters>
			</ContentProvider>
		</ContentProviders>
	</MapItem>

	<!--
  It is possible to create mapping nodes to point to action of controllers of desired areas. Also it possible to point to partial views in the "Partials" folders of themes.
  Mapping items can provide parameters to the content providers (actions and partial views).
  If the RenderAuto is called programmatically, there is a possibility to override the declarative parameter values.
  Any mapping item can contain one or more content provider definition. The content provider definitions can be of type Action or View or they can point to the same provider with different parameters.
  In case of having more than one content provider per mapping item, the input parameters will be distributed among them based on their position i.e., first content provider in the mapping item gets first parameter set in the list and so on.

  Next steps: 22.01.13 Javad
  The layout manager shows this xml file in a proper way and also the list of views in Partials folder and use can choose.
  Or she can enter the area/ controller/ action/ parameter, and so on from a list. The list can be provided by reflecting on Controller classes.
  Display information may come form i18n service or from Type Descriptors!
  -->
</Map>